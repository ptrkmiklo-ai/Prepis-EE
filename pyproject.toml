[project]
name = "prepis-ee"
version = "1.0.0"
requires-python = ">=3.11"

dependencies = [
    "streamlit",
    "pandas",
    "pymupdf>=1.22.0",
    "pypdf2",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
